# v0.5-alpha1 — WorkBuddy Release Smoke Report

> 状态：**PASS**
> 执行者：WorkBuddy / 真实宿主（WorkBuddy 5.6.2）
> 审计者：ChatGPT
>
> 这是 **release smoke**，不是 EXP-003。未重新测试 EXP-001 / EXP-002 已解决的问题；
> 本轮**未修改任何 Runtime / Registry 数据**（`src/`、`registry/*.json`、正式 Expert 名册全程零改动，见 §G / §I）。

## A. Frozen Target

- branch: `feat/v0.5a1-workbuddy-native-runtime`
- tested HEAD: `23be2f7fe0cd3c99deb6a637cca0f1f8501dde4f`
- PR: #9 — `OPEN` / **`draft=true`** / `MERGEABLE` / 30 files / 82 commits
- CI before smoke: run `35830580134` = `completed` / **`success`**，`headSha=23be2f7fe0cd3c99deb6a637cca0f1f8501dde4f`
- WorkBuddy version: **5.6.2**（`CFBundleShortVersionString` / `CFBundleVersion` 均为 5.6.2）
  - ⚠️ smoke 文档写的是 5.5.6，与实际不符 → 见 §H.1
- lead model: `DeepSeek-V4.1-Flash`（来源：会话上下文；**非**机器可读 —— `~/.workbuddy/settings.json` 与 subagent transcript 的 `providerData` 中均无 model 字段，见 §H.5）
- OS: macOS 26.5.2（build 25F84），arm64
- working tree: **clean** @ `23be2f7f`

## B. Install

- skill path: `~/.workbuddy/skills/zh-expert-os/SKILL.md`
- skill regular file?: **YES** — `Regular File`，非 symlink，4026 B，sha256 见下
- native agents path: `~/.workbuddy/agents/`
- native agents regular files?: **YES** — 7/7 全为 `Regular File`，**无任何 symlink**
- installer first run: `rc=0`，`native_agents_written=7`
- installer second run/idempotency: `rc=0`，`native_agents_written=0`（全部「已是最新」），
  **执行前后 8 个文件 sha256 逐一 diff 无差异** → IDEMPOTENT
- pre-existing conflict?: **YES，且 installer 行为正确** → 见下方 §B.1
- result: **PASS**

### B.1 初始 BLOCKED_BY_EXISTING_INSTALL（已由 Human Owner 决策解除）

installer 首次执行**报告冲突并 `exit 2`**，且**零副作用**（未创建 `~/.workbuddy/agents/`、
Skill sha256 与 mtime 未变、仓库树仍 clean）—— 与 preflight-first 设计一致。

被拒目标：`~/.workbuddy/skills/zh-expert-os/SKILL.md`

| 项 | 值（machine-read） |
|---|---|
| 类型 / 大小 | `Regular File` / 7061 B |
| mtime | `Sep 3 19:03:59 2026` |
| sha256（删除前的旧文件） | `2307cd0cf5e1a5a67db18619ecff44265b9395263f47a95c86c87744ad09753b` |
| 仓库内同内容文件 | `.claude/skills/zh-expert-os/SKILL.md`，sha256 **完全相同** |
| 性质 | 本项目自己的 **Claude Code 变体**（`# Zh Expert OS — Claude Skill`），被误放进 WorkBuddy skills 目录 |

按 spec「不得删除已有冲突文件来让安装通过 → 记录 BLOCKED_BY_EXISTING_INSTALL 并请求 Human Owner 决定」，
已停下并请求决策。**Human Owner 选择「直接删除（git 可恢复）」**。删除前清单（路径 / 类型 / 大小 /
mtime / sha256 / 恢复来源 / 删除理由）已归档到 B 层
`~/Desktop/项目思考/V0.5A1-evidence/pre-install/existing-skill-conflict-manifest.txt`。
删除后重跑 installer → `rc=0`。

**未覆盖任何用户内容**：旧文件与仓库文件字节一致，可从 git 无损恢复。

### B.2 安装保真校验（逐文件，machine）

仓库源（`adapters/workbuddy/`）与安装目标逐文件 sha256 全等：**8/8 `True`，`ANY symlink=False`**。

| 安装目标 | sha256 |
|---|---|
| `~/.workbuddy/skills/zh-expert-os/SKILL.md` | `3134a61d0da80617ae975b8dc8f563029f4a2575190420be1a36bdd8134b33db` |
| `~/.workbuddy/agents/zeos-router.md` | `967a42fdf04150728d71ebd4ca5e3b0d142b2130764e37c1ecb97a0ff8f38648` |
| `~/.workbuddy/agents/zeos-evidence.md` | `2ecafc5c594969f9be98cd05d0842131f63d08ede4548d233b4598989afcf922` |
| `~/.workbuddy/agents/zeos-red-team.md` | `b28000bd3fd4a0da0764fa3af73e84a0642ceda33c8affe5cf87977d866aa27e` |
| `~/.workbuddy/agents/zeos-auditor.md` | `56a7c88a87df2eba44884db2495c5bab769254ed58fcf8d597f1dc2307cdefaf` |
| `~/.workbuddy/agents/zeos-synthesizer.md` | `7f22ca66e4baec0daa606aca6026d275037178bf7ac47f711c1f2b3aa0c26103` |
| `~/.workbuddy/agents/zeos-recruiter.md` | `72c241720b9c262a5111e2d25276f809405bf5fe1d6266fbc536386b1f003124` |
| `~/.workbuddy/agents/zh-expert-os-lead.md` | `d3763daeea5abbd94e1388d9ea0823ffe4f7427162abc3bd0f610ac622239fc0` |

skill 目录内容差异：两种实现的 `zh-expert-os/SKILL.md` 大小与 sha256 不同（`7061` / `2307cd0c…` vs `4026` / `3134a61d…`），
确认替换的是不同产物，而非幂等误判。

## C. Router Native Invocation

- assignment_id: `smoke-router-001`
- host_task_id: `agent-33894946df9e4929`
- subagent_type: `zeos-router`
- host terminal status: `completed`
  - 证据：该 subagent transcript 末条 message 记录带 `status="completed"`；且 `Agent` 调用同步返回了最终回答
  - **未使用 running snapshot 作为终态判据**（见 §H.6）
- envelope task_id: `smoke-router-001`
- worker output path: `/tmp/zeos-v05-smoke/router-output.txt`（已归档 B 层；**从 transcript 机器提取，非手工转录**）
- validate-envelope command:

```bash
zh-expert-os workbuddy-validate-envelope \
  --file /tmp/zeos-v05-smoke/router-output.txt \
  --assignment-id smoke-router-001 \
  --agent-id zeos-router \
  --host-task-id agent-33894946df9e4929 \
  --host-status completed
```

- validate-envelope raw result: `rc=0`

```json
{
  "valid": true,
  "node_state": {
    "host_task_id": "agent-33894946df9e4929",
    "assignment_id": "smoke-router-001",
    "agent_id": "zeos-router",
    "host_status": "completed",
    "terminal": true
  }
}
```

（完整 JSON 含 `envelope` 全文，已归档 B 层 `smoke-io/router-validate-result.json`）

- host_task_id / assignment_id kept distinct?: **YES** —— `node_state` 中二者是**两个独立字段**
  （`agent-33894946df9e4929` vs `smoke-router-001`），未互相覆盖
- envelope 内**不含** host task id：`"39894946" in envelope_body == False`
  → child 未猜测、也未泄漏宿主 id，符合契约
- result: **PASS**

### C.1 补充负向控制（同一次 smoke 内，证明校验器非橡皮图章）

| 扰动 | 期望 | 实测 |
|---|---|---|
| `--assignment-id WRONG-ASSIGNMENT-999` | 拒绝 | `rc=1`，`ValueError: envelope.task_id='smoke-router-001' 与预期 assignment_id=… 不一致` |
| `--agent-id zeos-evidence` | 拒绝 | `rc=1`，`ValueError: envelope.agent_id='zeos-router' 与预期 agent_id='zeos-evidence' 不一致` |

## D. Evidence Artifact Provenance

- assignment_id: `smoke-evidence-001`
- host_task_id: `agent-398d0074b01649ec`
- namespace: `/tmp/zeos-v05-smoke/evidence`
- artifact relative path: `evidence.json`
- child artifact sha: `d4b525cbf5a705532d24b5b1ff64367926d0d89347ede055cc437520cf0053d2`
- claim evidence sha: `d4b525cbf5a705532d24b5b1ff64367926d0d89347ede055cc437520cf0053d2`
- lead machine-recomputed sha: `d4b525cbf5a705532d24b5b1ff64367926d0d89347ede055cc437520cf0053d2`
- all equal?: **YES** —— 三方（child `artifacts[0].sha256` / claim `evidence[0].artifact_sha256` / lead 独立重算）**完全一致**
- sibling path read?: **NO**
  - child 的 transcript `function_call` 序列只有 `['Bash', 'Write', 'Bash']`，**零 `Read`/`Glob`/`Grep`**
  - sibling 路径字符串（`zeos-v05-smoke/router`）在该 transcript 中**仅出现 1 次，位于 rec#0 = lead 自己派发的 prompt 禁令原文**；
    该字符串**不在 child 的最终输出中**
- validate-envelope raw result: `rc=0`

```json
{
  "valid": true,
  "node_state": {
    "host_task_id": "agent-398d0074b01649ec",
    "assignment_id": "smoke-evidence-001",
    "agent_id": "zeos-evidence",
    "host_status": "completed",
    "terminal": true,
    "artifacts": [
      { "relative_path": "evidence.json",
        "sha256": "d4b525cbf5a705532d24b5b1ff64367926d0d89347ede055cc437520cf0053d2" }
    ]
  }
}
```

- artifact 实体校验：`Regular File`、206 B、**非 symlink**，内容含 synthetic canary `ZEOS_V05_SMOKE_CANARY_71C9`（`True`）
- **未人工手打任何 hash**：三方比对全部由脚本从 transcript 原文与磁盘文件 machine-read
- result: **PASS**

## E. Registry → Native Export

- expert id: `router`
- registry status before: `governance`；`registry/experts.json` sha256 = `b5ff5f562cd1ee1a4b5f27c590d0f5c5f21fe7384623b538d4d5c0894de498a5`
- output path: `/tmp/zeos-v05-smoke/export/router.md`
- normal file?: **YES** —— `Regular File`、非 symlink、2495 B、sha256 `5ed8ab5a5b7be50611bbfc8d9fc2a8cef75b64dbeacaeb08dfbf54b8ac2ef0c1`
- name frontmatter: `name: router` ✓
- maxTurns compatibility metadata: `maxTurns: 200` ✓（**仅兼容元数据，不作预算闸门**）
- ZEOS_ENVELOPE contract present?: **YES** —— 含 `ZEOS_ENVELOPE` marker、`ZEOS_TASK_ID`、`ZEOS_ARTIFACT_NAMESPACE`、
  以及「不要猜 WorkBuddy host task_id」
- source/license leaked into execution prompt?: **NO**
  - `original` = 0、`MIT` = 0、`license` = 0
  - `source` 唯一 1 处命中是契约模板自身的字段 `"source_task_id"`（合法 schema 字段，非 Registry `source` 泄漏）
- registry status after: `governance`（**未变**）；`experts.json` sha256 `b5ff5f56…` **前后全等**
- candidate gate negative test: 见 §E.1
- export 原始 JSON：`native_hot_load_ready: true`；返回体显式声明「可调用不等于已晋升」
- result: **PASS**

### E.1 candidate governance gate

正式 registry **当前无 candidate**。按 spec「不要为了 smoke 修改正式 registry」，
改用**临时 Registry fixture**（`/tmp/zeos-v05-smoke/registry-fixture/registry/experts.json`，
复制正式 registry 后追加 `cand-smoke-001`，`status=candidate`），并从该 fixture root 运行 CLI。

| 用例 | 期望 | 实测 |
|---|---|---|
| `--expert cand-smoke-001`（candidate） | 拒绝 | **`rc=1`**，`WorkBuddyContractError: 只有 probation / active / governance Expert 可导出为 WorkBuddy native agent`；**输出目录 0 文件** |
| `--expert router`（同 fixture root，对照） | 成功 | `rc=0`，产出 `router.md` |

对照用例同时证明：CLI 确实读取的是 fixture root 的 registry（`find_root()` 自 cwd 上溯），
因此 candidate 的拒绝**源于 status 判定**，而非 registry 定位失败。

正式 registry 全程未被写入：`experts.json` / `matches.json` / `arena.json` sha256 三次采样一致。

## F. Top-level Skill Discovery

- current-session result: **HOT_DISCOVERED**
- if restart/new session tested: **NOT TESTED** —— 当前会话无法自证「重启后」状态（见 §H.3 的判据说明）
- evidence:
  - 直接按名加载成功：`Skill(skill="zh-expert-os")` 返回该 Skill 正文，且返回头声明
    `Base directory for this skill: ~/.workbuddy/skills/zh-expert-os`
  - 即宿主在**当前会话**即可从 `~/.workbuddy/skills/` 解析并加载该顶层 Skill
- result: **PASS**

### F.1 重要限定（不得据此宣称「自动发现已开启」）

宿主 `~/.workbuddy/settings.json` 的 `skillOverrides` 中，`zh-expert-os` 被标为
**`user-invocable-only`**（该 map 共 83 条，**全部**为该值）。

含义：该 Skill **可以按名直接调用**，但**不会出现在模型可见的 skill 自动索引里**
（对照：`skill-auditor` / `travel-cn` / `darwin-skill` / `markitdown-skill` / `product-manager` /
`skills-security-check` / `delivery-no-pseudoblock` 等不在该 map 中，故会出现在索引里）。
这也解释了 `~/.workbuddy/skills/` 下有 76 个含 `SKILL.md` 的目录、而模型侧仅见到少量条目。

- 该 override 是**宿主既有配置状态，不是本次安装写入的**（`install.sh` 只做 `cp`，不触碰 settings）。
- 因此本轮结论严格限定为：**「当前会话可按名直接加载」成立**；
  **「模型自动索引到该 Skill」未验证，且被既有宿主配置主动抑制**。
- 这不构成 release blocker（NOT_DISCOVERED 才是 blocker，而该情形已排除）。

## G. Regression

- unittest count: **56**
- unittest result: **OK（全绿，无 failure / error）**
  - 主跑：`~/.claude/skill-sources/zh-expert-os` 下 CI 同款 `python -m unittest discover -s tests -v` → `Ran 56 tests ... OK`
  - 交叉核验：另一解释器 + `PYTHONPATH=src` → `Ran 56 tests ... OK`
  - 其中包含 release 相关的 `test_workbuddy_package.WorkBuddyPackageTests`（installer 普通文件语义 /
    symlink 拒绝 / 产物语法 / shell installer 可解析）与 `test_workbuddy.WorkBuddyContractTests`
    （`maxTurns` 仅作兼容元数据、sha256 工具等）
- other checks:
  - `registry/experts.json`=`b5ff5f56…`、`matches.json`=`22f298c0…`、`arena.json`=`88a84abb…` —— 全程未变
  - repo 工作树 `git status --porcelain` 为空；HEAD 仍为 `23be2f7f`
  - **未修改任何 Runtime 代码**（smoke 期间 `src/` 一行未碰）

## H. Contradictions / Corrections

### H.1 宿主版本与 smoke 文档不一致（非阻塞，但需登记）

smoke 文档 Purpose 写的是「真实 WorkBuddy **5.5.6** 宿主」，实测宿主为 **5.6.2**。
`Info.plist` 的 `CFBundleShortVersionString` 与 `CFBundleVersion` 均为 `5.6.2`。

- 影响：EXP-001 / EXP-002 的结论均标注「限 5.5.6」。本轮所有宿主行为观测**实际发生在 5.6.2** 上。
- 处置：**未据此改写任何既有结论**；仅在本报告记录版本差异，供审计判断是否需要重新标注适用范围。
- 本轮未观测到与 EXP-002 结论冲突的宿主行为（TaskStop / 消息语义等本轮未重测）。

### H.2 初始 BLOCKED_BY_EXISTING_INSTALL

见 §B.1。属 spec 明确预设的情形；installer 行为**正确**（预检优先、拒绝覆盖、零副作用）。
已按 spec 停下并取得 Human Owner 决策，随后继续。**未通过删除冲突文件来让测试变绿** ——
删除是在明确授权与证据归档之后进行的。

### H.3 当前会话 team runtime.json 不存在（与 EXP-002 的采集方式不同）

EXP-002 时可从 `~/.workbuddy/teams/_auto_<sessionId>/runtime.json` 读取成员的 `taskId` / `status`。
本轮该目录**不存在**（`~/.workbuddy/teams/` 下仅存历史团队目录）。

因此本轮的 host identity 证据链改用：

1. **控制面返回值**（`Agent` 返回 `agent-33894946df9e4929` / `agent-398d0074b01649ec`）作为 host_task_id；
2. **subagent transcript** 的末条 message 记录 `status="completed"` 作为终态。

**未**用任何 running snapshot 推断终态。此变化属宿主侧状态差异，已如实登记，
不排除在后续轮次中 runtime.json 重新出现。

### H.4 托管 Python 环境缺 `setuptools`（环境限制，非仓库缺陷）

按 spec 执行 `python -m pip install -e . --no-build-isolation` 时失败：

```
BackendUnavailable: Cannot import 'setuptools.build_meta'
```

判定为**本机托管虚拟环境未安装 setuptools**，与仓库无关（CI 每次 `pip install -e .` 均通过）。
另确认 `zh-expert-os` CLI **已可用**：`/opt/miniconda3/bin/zh-expert-os`，
且 `import zh_expert_os` 解析到 `~/.claude/skill-sources/zh-expert-os/src/zh_expert_os/__init__.py`
—— 即**指向本仓库源码的 editable 安装**，随工作树分支实时生效。

本轮**未**在该环境额外安装第二份 CLI（避免 PATH 上出现两份可能分叉的可执行文件，
造成「实际跑的是哪个 CLI」的 provenance 歧义）。报告中所称 CLI 均指该 editable 安装。
未污染工作树（`pip install` 失败未产生任何文件）。

### H.5 lead model 无法从机器可读来源取得

`~/.workbuddy/settings.json`（顶层键：`autoLaunchLegacyCleanerRanAt` / `claw` /
`dockRecentAppsRepairedAt` / `enabledPlugins` / `officeFileAssociationsRepairMarker` /
`sandbox` / `skillOverrides`）与 subagent transcript 的 `providerData`
（内容仅 `{"agent": ..., "conversationRequestId": ...}`）中**均无 model 字段**。
故 §A 的 lead model 标注为会话上下文来源，**非**机器读取，避免伪装成实测值。

### H.6 终态判据合规声明

本轮全程遵守：running snapshot **未**被当作 terminal；`success:true` **未**被当作消息送达；
`TaskStop` **未**被当作 timeout；namespace **未**被称作 sandbox；
**未**把模型手打值回写文件/命令制造旁证。所有关键标量均可指回原始 tool result / transcript / artifact。

### H.7 非阻塞发现：CLI 对契约违约输出未捕获 traceback

对**预期内的**契约违约（assignment/agent 不匹配、candidate 导出被拒），CLI 直接抛出
Python `ValueError` / `WorkBuddyContractError` 的**完整 traceback**，而非结构化错误对象 + 退出码。
退出码与拒绝语义均正确，仅为输出形态问题。建议后续统一为结构化错误输出。
本轮**未**修改代码（smoke 阶段只报 blocker，不改 Runtime 让测试变绿）。

### H.8 执行侧自查：本轮无伪造记录

所有 sha256 均来自 `shasum` / `hashlib` 的真实读取；报告内 hash 写入后另做机器回校（见下）。
无任何自造观测值进入证据链。

> 报告 hash 回校：写完本报告后，用脚本抽取报告内全部 64 位十六进制串，
> 与磁盘上对应文件的**实时** sha256 做集合比对，未匹配项须为空。

## I. Environment Changes / Cleanup

- persistent installed skill: `~/.workbuddy/skills/zh-expert-os/SKILL.md`（4026 B，`3134a61d…`）— **保留**（供后续知流 trial）
- persistent installed native agents: `~/.workbuddy/agents/` 下 7 个 `.md`（6 × `zeos-*` + `zh-expert-os-lead`）— **保留**
- tmp smoke directory removed?: **YES** —— `/tmp/zeos-v05-smoke/` 已删除，`/tmp` 下无任何 `zeos-*` 残留
- worker post-hoc shutdown performed?: **NO** —— 按 spec，未对已 terminal 的 smoke workers 做 post-hoc shutdown
- manual recovery needed?: **NO**
- 额外删除：`~/.workbuddy/skills/zh-expert-os/SKILL.md` 的**旧 Claude 变体**（7061 B，`2307cd0c…`），
  经 Human Owner 授权删除，可从仓库 `.claude/skills/zh-expert-os/SKILL.md` 无损恢复；
  清单归档于 B 层 `V0.5A1-evidence/pre-install/`
- uninstaller **未运行**（无需回滚）

**B 层证据**（本地保留，未入 public repo）：
`~/Desktop/项目思考/V0.5A1-evidence/` —— 2 份完整 subagent transcript（router / evidence）、
全部 CLI 原始 JSON 与 worker 原文、导出产物、candidate fixture、安装冲突清单。

## J. PASS Gate

- [x] installer regular-file semantics
- [x] router native invocation
- [x] router envelope validated
- [x] host_task_id != assignment identity semantics preserved
- [x] evidence native invocation
- [x] artifact hash independently verified
- [x] evidence envelope validated
- [x] Registry native export works
- [x] candidate gate holds
- [x] Skill discovery is HOT_DISCOVERED or RESTART_REQUIRED
- [x] tests green
- [x] no governance/runtime data mutation to fake success

## K. Final Result

`PASS`

- blockers: **NONE**
  - 过程中出现过唯一一次 BLOCKED_BY_EXISTING_INSTALL（§B.1），属 spec 预设情形，
    经 Human Owner 决策后解除，未通过篡改环境绕过。
- non-blocking findings:
  1. 宿主版本 5.6.2 ≠ smoke 文档所称 5.5.6（§H.1）—— 建议审计时确认 EXP-001/002 结论的适用范围标注。
  2. 当前会话无 team `runtime.json`，host 终态改由控制面返回值 + transcript 承载（§H.3）。
  3. 托管 Python 缺 `setuptools`；CLI 依赖既有的指向本仓库的 editable 安装（§H.4）。
  4. CLI 对预期内契约违约输出未捕获 traceback（§H.7）。
  5. `zh-expert-os` 在宿主 `skillOverrides` 中为 `user-invocable-only` —— 可调用但不进模型自动索引（§F.1）。
- recommendation for PR #9:
  **具备转 Ready 条件**（latest CI green + 本 smoke PASS）。
  按 `V0.5A1_CHATGPT_CODE_REVIEW.md` 的 Merge Rule，仍需 **ChatGPT 对本 smoke report 终审 PASS** 后方可转 Ready 并合并。
  **WorkBuddy 未自行 merge PR #9**，PR #9 保持 `draft`。
